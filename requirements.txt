# CPU-only analysis path (E1, E3, E4, E5, E6 and all statistical gates)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Additionally required by the end-to-end serving experiment (E2, needs a GPU):
#   torch, transformers, sentence-transformers, hnswlib
